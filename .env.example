# Copy to .env. Consumed by `docker compose` only.
# The API process reads appsettings.Development.json + `dotnet user-secrets` — do NOT load this file from .NET.

ConnectionStrings__AppDb=Host=localhost;Port=5432;Database=base_app;Username=postgres;Password=postgres
